"use client";

import React from "react";

export default function BuilderSettingsPage() {
  return (
    <main className="max-w-3xl mx-auto py-12">
      <h1 className="text-2xl font-semibold mb-4">Builder settings</h1>
      <p className="text-sm text-slate-500">
        This page will surface global settings for the Build With AI builder:
        defaults for typography, color palettes, AI behavior, and experimental
        features. For now, it is a placeholder to unblock routing and layout.
      </p>
    </main>
  );
}
